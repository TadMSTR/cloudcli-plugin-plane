{
  "name": "cloudcli-plugin-plane",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx tsc --noEmit && npx esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --sourcemap && npx esbuild src/server.ts --bundle --format=esm --platform=node --outfile=dist/server.js --sourcemap --external:ws"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.4",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "ws": "^8.20.0"
  }
}
